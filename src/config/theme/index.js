import header from './header';
import button from './button';
import { input, label, checkbox } from './inputs';

const theme = {
	header,
	button,
	input,
	label,
	checkbox
};

export default theme;
